services:
  admin:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL}
    image: goldenhive-admin:local
    container_name: goldenhive-admin
    ports:
      - "5173:80"
    restart: unless-stopped
